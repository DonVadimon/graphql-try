import styled from 'styled-components';

export const Button = styled.button`
    background: #00fdbe;
    padding: 12px;
    font-size: 24px;
    color: white;
    border-radius: 8px;
    border: none;
    outline: none;
    transition: all linear 100ms;
    cursor: pointer;

    &:hover {
        background: #1ad1a3;
    }
`;
